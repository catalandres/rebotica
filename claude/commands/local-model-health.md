# /local-model-health

Check whether the configured provider is reachable through Rebotica.

Run:

```sh
rbtc health
```

If the model list is empty or the server is unreachable, ask the user to start the configured endpoint or set `REBOTICA_BASE_URL`.
