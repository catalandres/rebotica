# /local-worker-health

Check whether the configured provider is reachable through Atelier.

Run:

```sh
atelier health
```

If the model list is empty or the server is unreachable, ask the user to start the configured endpoint or set `ATELIER_BASE_URL`.
